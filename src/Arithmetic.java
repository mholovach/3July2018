public interface Arithmetic {
    Integer plusPlus(int one, int two);
    Integer minusMinus(int one, int two);
    Integer multiplyM(int one, int two);
    Integer divideD(int one, int two);
    String result (String res);
}
